# Film App With React & Redux


## Available Scripts

First of all you need to install packages by running:

### `npm install` or  `npm i`

In the project directory, you can run:

### `npm run start`

